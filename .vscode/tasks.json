{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Package Driver",
      "type": "shell",
      "command": "node ${workspaceFolder}/scripts/zip.js ${fileDirname} ${workspaceFolder}/zip",
      "windows": {
          "command": "node ${workspaceFolder}\\scripts\\zip.js ${fileDirname} ${workspaceFolder}\\zip"
      },
    }
  ]
}